# 2026-07-13 — v1.0.1 修复迭代

## 修复
- [x] 状态保存：改回标准 APVTS `copyState()/replaceState()` 模式，不再手动序列化
- [x] UI 缩放：zoom 按钮现在同时调整 JUCE 窗口大小 (`setSize()`)，不再只缩内容不缩边框
- [x] 缩放按钮位置：`right:90px` → `right:130px`，往左挪
- [x] zoom 值统一为 1.324 基准（与 CSS body transform 一致）
- [x] README 补回算法来源说明（AirWindows: ButterComp2, PurestDrive, SmoothEQ3）

## 待测
- 用户需验证：DAW 重开工程参数是否保留
- 用户需验证：zoom 按钮是否同时缩放窗口 + 内容
